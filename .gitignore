# --- Security ---
# Keep the real Firebase configuration local. The app and widget Info.plist
# files are source-controlled because they define each target's metadata.
ViRest/GoogleService-Info.plist
*.env

# --- Xcode/Mac noise ---
.DS_Store
DerivedData/
*.xcuserstate
*.xcuserdatad
xcuserdata/
